{
  "name": "EBS-API",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "debug": "twilio-run --inspect",
    "deploy": "twilio serverless:deploy --override-existing-project",
    "local": "twilio serverless:start"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "node": "^12.22.1",
    "twilio": "^3.56",
    "twilio-flex-token-validator": "^1.5.6",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "twilio-run": "^2.6.0"
  },
  "engines": {
    "node": "12"
  }
}
